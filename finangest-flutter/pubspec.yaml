name: finangest
description: Sistema profesional de gestión de cobranzas
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase (Plan Spark - 100% Gratis) - Versiones actualizadas para web
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  google_sign_in: ^6.1.6
  
  # State Management
  provider: ^6.1.1
  
  # UI
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  table_calendar: ^3.0.9
  fl_chart: ^0.65.0
  
  # Utils
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.2
  path_provider: ^2.1.1
  
  # Security
  crypto: ^3.0.3
  
  # PDF/CSV Export
  pdf: ^3.10.7
  csv: ^5.1.1
  
  # Icons
  cupertino_icons: ^1.0.6
  font_awesome_flutter: ^10.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/translations/